# Reference only — a plain static site (no build step) can't read
# .env files at runtime, so the same values are set directly in
# supabase-client.js. Keep this file if you later move Knot into a
# bundler (Vite/Next.js) that does support env vars.

NEXT_PUBLIC_SUPABASE_URL=https://wjtllmdscjmvzdybyexu.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_rgc8Kckq-q0iKQU-rVeiXQ_N6n7p77_
